import { GetUserBasicInfos, postUserInfos } from "./routes/user";

// TODO: Delete this

const testDemo = () => {
  const value = null;

  const response = GetUserBasicInfos();

  // utiliser le get
};
